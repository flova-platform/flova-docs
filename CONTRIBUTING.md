# راهنمای مشارکت

این سایت برای کسی نوشته می‌شود که می‌خواهد یک کار واقعی را تمام کند؛ متن کوتاه،
انسانی و مرحله‌به‌مرحله باشد.

## قواعد محتوا

- نثر و navigation فارسی و RTL باشد.
- کد، مسیر API، کلید فنی و query را ترجمه نکنید؛ آن‌ها را LTR نگه دارید.
- قبل از توضیح یک اصطلاح، معنی انسانی آن را بگویید.
- فقط قابلیت عمومی و قابل‌بررسی را مستند کنید.
- ID، token، credential، host خصوصی، دادهٔ مشتری و آمار ساختگی ممنوع است.
- اگر قابلیتی هنوز عمومی یا verify نشده، آن را وعدهٔ قطعی ننویسید.
- مثال‌ها باید کوچک، قابل‌کپی و دارای placeholder روشن باشند.

## تغییر API یا SDK

قرارداد public را از منبع canonical بررسی کنید و snapshotهای `openapi/` را همراه
با توضیح انسانی و مثال‌های مرتبط به‌روزرسانی کنید. تغییرات متناقض بین API،
کنسول و SDK باید پیش از انتشار حل شوند.

## بررسی محلی

```bash
mint validate --check-redirects
mint a11y
```

همچنین صفحهٔ تغییرکرده را در حالت روشن و تاریک، در viewport موبایل و دسکتاپ، و
با focus صفحه‌کلید بررسی کنید. موفقیت validation جای review واقعی RTL را نمی‌گیرد.
